import React from 'react';


export default function AddTownInfo() {
  const [formData, setFormData] = useState({
    gamename: '',
    townname: '',
    hemisphere: '',
    nativefruit: '',
    user_id:'',

  })

  const { gamename } = formData 
  const { townname } = formData
  const { createSubmit } = props 
  
}